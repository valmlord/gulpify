const parameters = {
  htmlmin: {
    collapseWhitespace: true,
    removeComments: true,
  },
  autoprefixer: {
    overrideBrowserslist: ['last 3 versions'],
    cascade: false,
  },
};

export default parameters;
